---
title: JWT Strategy
description: Implementing JSON Web Tokens for authentication.
---

# JWT Strategy

The JSON Web Token (JWT) strategy is implemented using `jsonwebtoken` library.

## How it works

1. User logs in.
2. Server issues a signed JWT.
3. Client stores JWT (e.g., in localStorage or httpOnly cookie).
4. Client sends JWT on subsequent requests.
